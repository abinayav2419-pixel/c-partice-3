#include <stdio.h>
int multipletable(int number){
    int i;
    for(i = 1; i <= 10; i++){
        printf("%d x %d = %d\n", number, i, number * i);
    }
}
int main(){
    int number;
    scanf("%d", &number);
    multipletable(number);

    return 0;
}