#include <stdio.h>
int main() {
    char source[100], dest[100]; 
    int i = 0; 
    scanf("%s", source); 
    while(source[i] != '\0') {
        dest[i] = source[i];
        i++;
        } 
        dest[i] = '\0';
        printf("%s", dest);
      return 0;
}



