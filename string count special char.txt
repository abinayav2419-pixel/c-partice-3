#include <stdio.h>
#include <ctype.h>
int main() {
    char str[100];
    int count = 0, i = 0;
    scanf("%s", str);
    while(str[i] != '\0') {
        if(!isalnum(str[i])) {
            count++;
        }
        i++;
    }

    printf("%d", count);

    return 0;
}