#include <stdio.h>
#include <string.h>
int main() {
    char id[100];
    int i, count = 0;
    scanf("%s", id);

    for(i = 0; i < strlen(id); i++) {
        if(id[i] >= '0' && id[i] <= '9') {
            count++;
        }
    }

    printf("%d\n", count);

    return 0;
}